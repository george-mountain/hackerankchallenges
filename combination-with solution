# Enter your code here. Read input from STDIN. Print output to STDOUT
from itertools import combinations_with_replacement as cwr
S,k = input().split()
comb = list(cwr(sorted(S),int(k)))
result = ["".join(ele) for ele in comb]
for value in result:
    
    print(value)
